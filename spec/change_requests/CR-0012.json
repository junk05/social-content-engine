{
  "schema_version": 1,
  "change_request_id": "CR-0012",
  "status": "IMPLEMENTED_HG_REQUIRED",
  "title": "Calibrate DOM-to-Quartz screen coordinates",
  "risk": "HIGH",
  "affected_components": ["Chrome content geometry probe", "loopback receiver", "macOS Quartz helper"],
  "approved_decision": "Use one move-only cursor calibration before one separately gated click with the existing Quartz mechanism.",
  "unchanged_boundaries": ["one selected post", "one worker tab", "no click before alignment gate", "no alternate input method", "no batch", "no M5"]
}
