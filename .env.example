# Obtain through the Meta Threads OAuth flow. Never commit a real token.
THREADS_ACCESS_TOKEN=

# Meta callback server. Never commit real values.
THREADS_APP_SECRET=
META_OAUTH_STATE=
META_PUBLIC_BASE_URL=https://replace-with-temporary-tunnel.example
META_CALLBACK_HOST=127.0.0.1
META_CALLBACK_PORT=8787
